# Basic scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
psutil>=5.8.0

# Single-cell analysis
scanpy>=1.9.0
muon>=0.1.0
anndata>=0.8.0
h5py>=3.7.0
tables>=3.7.0

# Machine learning
scikit-learn>=1.0.0

# GPU computing (try these in order)
cupy-cuda12x>=12.0.0
# If above fails, try: cupy-cuda11x>=11.0.0
# If above fails, try: cupy

# GPU ML (try these in order) 
cuml-cu12
# If above fails, try: cuml-cu11
# If above fails, try: rapidsai-nightly --extra-index-url=https://pypi.anaconda.org/rapidsai-wheel-nightly/simple

# Utilities
pathlib2